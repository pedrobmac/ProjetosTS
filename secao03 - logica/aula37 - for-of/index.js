// retorna o valor em si (iteráveis, array, string)

nome = "Maria Silva";

for(let letra of nome){
    console.log(letra);
}

//objeto não é iterável (não tem índice, tem chave), então não se usa pra esse tipo

